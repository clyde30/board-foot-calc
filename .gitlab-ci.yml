stages:
  # - build
  - deploy

# build-job:
#   stage: build
#   only:
#     - develop
#   script: npm install 

deploy-job:
  stage: deploy
  only:
    - develop
  script: ls && pwd && npm install && rm -rf /build && npm run build
